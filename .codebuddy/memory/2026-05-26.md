# 2026-05-26

## Broker 产品化完成
- **server.py 重构**（~300 行）：
  - 新增 `/api/v1/system/capabilities` 端点，枚举全部探针能力
  - 新增 `/api/v1/system/shutdown` 端点，支持 SIGTERM 优雅关闭
  - 统一错误格式：HTTPException → `{"status":"error","code":"BROKER_HTTP_{code}","message":"..."}`
  - 所有 10 个路由增加 `asyncio.wait_for()` 超时保护（3-10 秒按探针类型）
  - 新增请求日志中间件（方法/路径/状态/耗时）
- **run.py 修改**：Token 脱敏显示（首尾 8 位）、信号处理注册
- **新建 `deploy/com.asu.broker.plist`**：LaunchAgent 模板，开机自启 + 崩溃重启
- **新建 `scripts/install_broker_daemon.sh`**：一键部署脚本，含 Token 探活
- **新建 `scripts/uninstall_broker_daemon.sh`**：一键卸载脚本

## 项目进度评估
- 整体完成度 ~75-80%
- Broker 现已达到产品化水平（常驻部署 + 能力发现 + 超时保护 + 统一错误）
- 下一步优先级：IDE Extension v2（/selection、/diagnostics、/git-diff）

## 文档统一更新
修复 8 份文档中的过时与矛盾描述：
- **ASU_Custom_Agent_Guide.md**：修正 Agent 生命周期描述（UI 不再拉起子进程）、补充 SQLite 持久化、Persona 文件化
- **ASU_Local_Agent_Roadmap.md**：更新 Broker 产品化状态、打勾已完成项
- **ASU_Next_Gen_Roadmap.md**：P0.1 标记完成、P2.3 标记完成、更新 Provider 描述
- **ASU_Broker_Development_Guide.md**：更新 API 列表和目录结构、部署章节改为已实现
- **ASU_Daemon_Deployment_Plan.md**：补充 Broker 常驻化、更新命令速查表
- **ASU_Architecture_Context_Extraction.md**：更新阶段七-B 状态说明
- **README.md**：路线图新增 Broker 产品化行、修正上下文窗口状态、更新项目结构
- **MEMORY.md**：更新四层架构、Broker 状态、当前分支、待办

## Broker 产品化 + 文档修订 + 极端验证

### 本轮完整产出
- Broker 产品化（LaunchAgent + capabilities/shutdown + 超时保护 + 错误统一 + Token 脱敏）
- 文档全文联动修订功能（persona + UI + 协议 + Broker office 解析）
- 10 个真实案例验证（4 基础 + 4 极端 + 2 Broker 解析），0 误报，100% 通过
- 8 份文档全面对齐，去除过时矛盾描述
- Broker 支持 .docx/.pptx 解析（新增 office_probe.py）
- 测试报告 `test_docs/TEST_REPORT.md`（425 行，含全量案例详情）
