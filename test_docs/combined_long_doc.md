--- 文档: test_docs/budget_report.md ---
# 2025 年度市场部预算执行报告

## 一、总体执行情况

2025 年度市场部总预算 800 万元，截至 Q3 已执行 580 万元，执行率 72.5%。各季度分项如下：

| 季度 | 预算（万元） | 实际支出（万元） | 执行率 |
|------|-------------|-----------------|--------|
| Q1 | 200 | 185 | 92.5% |
| Q2 | 200 | 210 | 105.0% |
| Q3 | 200 | 185 | 92.5% |
| Q4 | 200 | 0 | 0% |

## 二、各渠道投放详情

### 2.1 线上投放
- 搜索引擎广告（SEM）：预算 320 万元，已执行 230 万元
- 信息流广告：预算 160 万元，已执行 140 万元
- KOL 合作：预算 120 万元，已执行 90 万元

### 2.2 线下活动
- 行业展会参展：预算 120 万元，已执行 80 万元
- 客户答谢会：预算 80 万元，已执行 40 万元

## 三、Q4 剩余预算分配

Q4 剩余可用预算 220 万元。根据 Q3 数据复盘，建议优化如下：

| 渠道 | 原 Q4 预算 | 建议调整 | 调整原因 |
|------|-----------|----------|----------|
| SEM | 80 | 60 | ROI 从 3.2 降至 2.8 |
| 信息流 | 40 | 50 | Q3 转化率提升 15% |
| KOL | 30 | 40 | 双11/双12 促销周期 |
| 展会 | 30 | 40 | 年末两场大型展会 |
| 答谢会 | 20 | 30 | 新增 VIP 客户答谢 |

## 四、审批与执行

上述调整方案需经 VP 审批（负责人：王建国），审批通过后由财务部（对接人：赵经理）在 11 月 15 日前完成预算划拨。

**关键风险**：若 Q4 SEM 预算削减后 ROI 继续下滑，全年市场部 ROI 可能跌破 2.5 的 KPI 底线。


--- 文档: test_docs/meeting_notes.md ---
# 2025 Q3 产品线复盘会议纪要

## 会议信息
- **时间**：2025-09-15 14:00-16:00
- **地点**：3 号会议室 + 线上 Zoom
- **主持人**：陈总（产品总监）
- **参会人**：王磊（前端 Lead）、赵敏（后端 Lead）、刘洋（测试负责人）、周婷（运营）、马超（架构师）

## 一、智能客服系统

### 1.1 当前进展
智能客服 Bot V2 已于 8 月底上线，目前覆盖 80% 的常规咨询场景。用户满意度从 V1 的 3.2 分提升至 4.1 分。

### 1.2 遗留问题
- 对于需要人工介入的复杂场景（退款纠纷、投诉处理），转接流程仍不顺畅，平均转接耗时 45 秒
- 知识库更新依赖于人工维护，新增 FAQ 需要 2-3 个工作日才能生效

### 1.3 Q4 计划
- 完成转接流程优化，目标将转接耗时降至 25 秒以内
- 引入自动知识库更新机制

## 二、数据看板系统

### 2.1 当前进展
运营数据看板 V3 已交付，支持实时数据刷新和自定义图表配置。日均活跃使用者 120 人。

### 2.2 遗留问题
- 大屏展示模式下，部分低配机器出现卡顿现象（Chrome 内存占用超过 1.5GB）
- 数据导出功能仅支持 CSV 格式，运营团队反馈需要 Excel 导出

### 2.3 Q4 计划
- 优化大屏渲染性能，目标将 Chrome 内存占用降至 1GB 以内
- 新增 Excel 导出功能

## 三、权限管理系统

### 3.1 当前进展
RBAC 权限管理模块已完成开发，正在内网灰度测试中。已覆盖组织架构 80% 的部门。

### 3.2 遗留问题
- 审批流程中的角色继承逻辑在某些边缘场景下会产生权限泄漏
- 与第三方 SSO 的集成联调预计还需要 4 周

### 3.3 Q4 计划
- 修复权限泄漏漏洞
- 完成 SSO 集成并全量上线

## 四、团队与资源

当前研发团队总人数 35 人。Q3 有 2 位同事离职（前端张三、后端李四），预计 Q4 可补充 3 位新同事。服务器成本 Q3 累计 18 万元，Q4 预算 22 万元。


--- 文档: test_docs/tech_spec.md ---
# OpenCopilot 技术规格说明书

## 1. 产品定义

OpenCopilot（简称 OpenCopilot）是一款面向 macOS 平台的桌面级 AI 智能协驾工具。通过全局鼠标监听和悬浮卡片交互，实现跨应用的上下文感知和智能分析。

## 2. 核心模块

### 2.1 交互层 (smart_copilot.py)
采用 PyQt6 双图层架构，全屏穿透特效层负责光标视觉效果，局部可交互卡片层承载 AI 对话。支持双击右键唤出快捷卡片，三击右键唤出任务工作台。

### 2.2 智能体层 (asu_custom_agent.py)
运行于端口 18888 的独立 HTTP 服务，负责 LLM 会话管理、角色切换、上下文记忆和模型调度。目前支持 MiniMax 和本地 OpenAI-compatible 两种后端。

### 2.3 上下文层
- **IDE 插件 (asu-ide-extension/)**：基于 VS Code Extension API，通过 HTTP 暴露当前文件全文和选区
- **特权代理 (asu_broker/)**：运行于端口 18889，持有 macOS 系统高级权限，提供浏览器 DOM 提取、屏幕截图、剪贴板读写等探针能力

### 2.4 模型层 (llm_provider.py)
抽象 Provider 接口，支持 MiniMax M2.7（云端）和多款本地模型（Ollama/vLLM）的动态切换。

## 3. 通信架构

```
UI (PyQt6) ←→ Agent Server (127.0.0.1:18888) ←→ LLM Provider
    ↓                      ↓
 SystemProbeClient ←→ Broker (127.0.0.1:18889) → macOS System APIs
    ↓
 IDE Extension ←→ VS Code API
```

## 4. 性能要求

| 指标 | 目标值 | 当前状态 |
|------|--------|----------|
| 卡片唤出延迟 | < 200ms | ✅ 150ms |
| AI 首字响应 | < 3s | ✅ 2.1s (MiniMax) |
| 内存占用 | < 500MB | ✅ 320MB |
| CPU 空闲占用 | < 2% | ✅ 1.2% |
| 上下文窗口 | 30K tokens | ✅ 已实现 |

## 5. 部署方案

推荐通过 macOS LaunchAgent 实现开机自启。Agent 和 Broker 分别注册为独立守护进程，UI 作为无状态客户端按需唤出。日志统一输出至 ~/Library/Logs/ASU/。


--- 文档: test_docs/project_plan.md ---
# 智能服务中台 Q3 项目规划书

## 1. 项目概述

智能服务中台（IS-Platform）是一套为企业数字化转型提供基础能力的中间件系统。项目自 2025 年 Q1 启动，目前已完成核心架构搭建和首个试点客户的上线交付。

## 2. 当前里程碑

| 里程碑 | 计划完成时间 | 状态 |
|--------|-------------|------|
| M1: 核心架构搭建 | 2025 Q1 | ✅ 已完成 |
| M2: 试点客户 A 上线 | 2025 Q2 | ✅ 已完成 |
| M3: 客户 B 与客户 C 并行接入 | 2025 Q4 | 🔶 进行中 |
| M4: 全功能稳定版发布 | 2026 Q1 | ⏳ 未开始 |

## 3. 资源与预算

项目团队目前由 8 名工程师组成，其中：
- 前端开发 2 人
- 后端开发 4 人
- 测试 1 人
- DevOps 1 人

Q3 季度预算审批中，预计总预算 120 万元。项目总负责人为张伟。

## 4. 关键风险

1. 客户 C 对私有化部署的要求远超预期，可能需要进行架构层面的适配改造。
2. 团队中资深后端工程师李明的离职（预计 2025 Q3 末）可能导致知识断层。
3. 预算审批流程延迟可能导致 Q4 资源不足，影响 M3 里程碑的正常推进。

## 5. 技术选型

当前技术栈：
- 后端：Python FastAPI + PostgreSQL + Redis
- 前端：React 18 + Ant Design Pro
- 基础设施：Kubernetes + Istio + ArgoCD
- 监控：Prometheus + Grafana + ELK

## 6. 下一步计划

Q4 的重点是完成 M3 里程碑（客户 B 与客户 C 接入），同时启动 M4 的前期技术预研。


--- 文档: test_docs/api_spec.md ---
# Payment Gateway API v2.3 接口文档

## 1. 概述

Payment Gateway（支付网关）是电商平台核心交易系统的统一支付入口。当前版本 v2.3，支持微信支付、支付宝、银联云闪付三种支付渠道。

## 2. 接口列表

### 2.1 创建订单
```
POST /api/v2/order/create
```
创建支付订单。必传参数：
- `merchant_id`：商户号
- `amount`：订单金额（单位：分）
- `channel`：支付渠道（WECHAT / ALIPAY / UNIONPAY）
- `notify_url`：异步回调地址

### 2.2 查询订单
```
POST /api/v2/order/query
```
根据 `order_id` 或 `merchant_order_no` 查询订单状态。

### 2.3 发起退款
```
POST /api/v2/refund/create
```
创建退款单。注意：部分退款金额不得大于原订单金额。

### 2.4 查询退款
```
POST /api/v2/refund/query
```
根据 `refund_id` 或 `order_id` 查询退款状态。

## 3. 签名算法

所有请求需附带 `sign` 字段。签名步骤：
1. 将请求参数按 key 字典序排序
2. 拼接为 `key1=value1&key2=value2` 格式
3. 末尾追加 `&key=MERCHANT_SECRET`（商户密钥）
4. 计算 MD5 哈希值，转大写

## 4. 错误码

| 错误码 | 含义 |
|--------|------|
| 1001 | 参数缺失 |
| 1002 | 签名验证失败 |
| 2001 | 商户不存在 |
| 2002 | 商户余额不足 |
| 3001 | 订单不存在 |
| 3002 | 订单已关闭 |
| 4001 | 退款金额超限 |

## 5. 注意事项

- 同一订单号不可重复创建订单
- 退款接口仅支持原订单创建后 90 天内的交易
- 测试环境商户密钥统一为 `TEST_SECRET_KEY_2025`
